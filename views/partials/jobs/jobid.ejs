<% /*
    When you have job id, shows the job listing.
*/
    locals.job = all_ids[jobId];
%>

<% if(locals.in_select) { %>

    <%- partial('jobs/job-select', { locals: locals} ) %>

<% } else { %>
    
    <%- partial('jobs/job', { locals: locals} ) %>
    
<% } %>