<% /*
    All Jobs
    http://localhost:3000

    Currently also used for search results.

    This file must be the list of all jobs.
    If we want to change the home page to something else tell Dylan and he will make a new ejs.
*/ %>

<div class="content">

    <div class="span-17 first last">
        <h2>All Openings</h2>
        <h3><br /></h3>
        <% if (jobs['dc-northern-virginia']) { %>
            <div class="span-4 first">
                <h4>Arlington, VA</h4>
            </div>
            <div class="span-13 last job-section">
                <%- partial(locals.teams ? 'jobs/team' : 'jobs/jobs', { collection: jobs['dc-northern-virginia'], as: "jobs", locals: { location: 'dc-northern-virgina' }} ) %>
            </div>
        <% } %>

        <% if (jobs['san-francisco']) { %>
            <div class="span-4 first">
                <h4>San Francisco, CA</h4>
            </div>
            <div class="span-13 last job-section">
                <%- partial(locals.teams ? 'jobs/team' : 'jobs/jobs', { collection: jobs['san-francisco'], as: "jobs", locals: { location: 'san-francisco' }} ) %>
            </div>
        <% }  %>
    </div>

</div>

